<%= form_for(@project) do |t| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@project.errors.count, "error") %> prohibited this entry from being saved:</h3>
      <ul>
        <% @project.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= t.label :name %><br>
    <%= t.text_field :name %>
  </div>
  <br>
  <div class="actions">
    <%= t.submit %>
  </div>
<% end %>

